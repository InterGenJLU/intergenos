<!-- D-Bus system-bus policy for the Forge installer backend service. -->
<!-- Installs to /usr/share/dbus-1/system.d/. -->
<!DOCTYPE busconfig PUBLIC
 "-//freedesktop//DTD D-BUS Bus Configuration 1.0//EN"
 "http://www.freedesktop.org/standards/dbus/1.0/busconfig.dtd">
<busconfig>
  <!-- Only root may own the service name (this is the backend itself). -->
  <policy user="root">
    <allow own="org.intergenos.ForgeInstaller1"/>
    <allow send_destination="org.intergenos.ForgeInstaller1"/>
    <allow receive_sender="org.intergenos.ForgeInstaller1"/>
  </policy>

  <!-- Live ISO: the `intergenos` user (sudo-capable, present only on live
       media per D-007) may invoke the install backend. On installed
       systems no `intergenos` account exists, so this rule never matches
       and the policy below takes effect. -->
  <policy user="intergenos">
    <allow send_destination="org.intergenos.ForgeInstaller1"
           send_interface="org.intergenos.ForgeInstaller1"/>
    <allow receive_sender="org.intergenos.ForgeInstaller1"/>
  </policy>

  <!-- Installed systems: members of `wheel` may invoke (Forge's "re-install"
       or "modify" entry point requires elevated privileges anyway, gated
       by polkit auth_admin_keep on the install method per the future
       installed-system invocation path). Conservative wheel-only default;
       can tighten further once that flow lands. -->
  <policy group="wheel">
    <allow send_destination="org.intergenos.ForgeInstaller1"
           send_interface="org.intergenos.ForgeInstaller1"/>
    <allow receive_sender="org.intergenos.ForgeInstaller1"/>
  </policy>

  <!-- Default-deny for everyone else. -->
  <policy context="default">
    <deny send_destination="org.intergenos.ForgeInstaller1"/>
    <deny receive_sender="org.intergenos.ForgeInstaller1"/>
  </policy>
</busconfig>
