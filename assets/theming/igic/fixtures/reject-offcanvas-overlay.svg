<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 256 256">
  <!-- Reject fixture: pins the off-canvas-overlay clip class (Decided 2026-07-23).
       A plausible in-band body PLUS an overlay ellipsis authored ABOVE the canvas —
       the exact shape of the acquiring-state regression: the standard raster render
       hides the dots, the padded sweep must see them. sweep_clip.py MUST exit 1 on
       this file; a gate battery that passes it is broken. -->
  <g fill="none" stroke="#e2e8f0" stroke-width="8" stroke-linecap="round">
    <path d="M 64 96 L 192 96 L 192 208 L 64 208 Z"/>
    <path d="M 104 208 L 104 240"/>
    <path d="M 152 208 L 152 240"/>
  </g>
  <g fill="#0099FF">
    <circle cx="88" cy="-24" r="12"/>
    <circle cx="128" cy="-24" r="12"/>
    <circle cx="168" cy="-24" r="12"/>
  </g>
</svg>
