name: which
version: "2.23"
release: 1
description: Utility to show the full path of commands
license: GPL-3.0-or-later
homepage: https://www.gnu.org/software/which/
tier: base
build_style: autotools

source:
  - url: https://ftpmirror.gnu.org/which/which-${version}.tar.gz
    sha256: placeholder-which-2.23

dependencies:
  build: []
  host: []
  runtime: []

configure_flags:
  - "--prefix=/usr"
