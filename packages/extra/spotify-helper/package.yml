name: spotify-helper
version: "1.0"
release: 1
description: Helper to download and install Spotify
license: GPL-3.0-or-later
payload_license: LicenseRef-Spotify-ToS
homepage: https://www.spotify.com/
tier: extra
build_style: custom
install_func: do_install
source: []
dependencies:
  build: []
  host: []
  runtime:
  # H-007 Phase B: spotify-helper sources /usr/share/igos/helpers/helper-lib.sh.
  - intergenos-helper-lib
verify_paths:
  - /usr/bin/igos-install-spotify
