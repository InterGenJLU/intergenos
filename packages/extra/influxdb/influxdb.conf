# InfluxDB Core configuration skeleton
# Bind localhost only — operators edit to expose via network
# Python plugin runtime OFF by default

http-bind-address = "127.0.0.1:8086"
storage-engine-path = "/var/lib/influxdb"
