--- a/CMakeLists.txt
+++ b/CMakeLists.txt
@@ -210,7 +210,12 @@
     )
   else()
     add_custom_command(OUTPUT "${AOTRITON_TRITON_SO}"
-      COMMAND ${CMAKE_COMMAND} -E env TRITON_BUILD_PROTON=OFF VIRTUAL_ENV=${VENV_DIR} TRITON_BUILD_DIR=${TRITON_BUILD_DIR} "${VENV_BIN_PYTHON}" -m pip install .
+      # IGOS: explicit offline flags on the bundled-triton install. The
+      # first burn proved the PIP_NO_BUILD_ISOLATION / PIP_NO_INDEX
+      # environment did not govern this ninja-time pip invocation (it ran
+      # an isolated build-dep resolution and died index-less on
+      # setuptools>=40.8.0); argv flags cannot be lost by any env path.
+      COMMAND ${CMAKE_COMMAND} -E env TRITON_BUILD_PROTON=OFF VIRTUAL_ENV=${VENV_DIR} TRITON_BUILD_DIR=${TRITON_BUILD_DIR} "${VENV_BIN_PYTHON}" -m pip install --no-build-isolation --no-index .
       WORKING_DIRECTORY "${CMAKE_CURRENT_LIST_DIR}/third_party/triton/"
     )
   endif()
