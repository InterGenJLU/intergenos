name: rocrand
version: "7.2.4"
release: 1
description: ROCm random number generation library (device-side RNG for ML training and simulation)
license: MIT
homepage: https://github.com/ROCm/rocm-libraries
tier: compute
build_style: custom
install_func: do_install

# SAME rocm-libraries tarball (shared-source pin); builds
# projects/rocrand from the monorepo. Compiled device library — the
# declared gpu_targets set is load-bearing (kernels compile for exactly
# these). BUILD_FORTRAN_WRAPPER stays at its upstream default OFF (a
# deprecated legacy interface no reference distro ships — not a Rule-3
# dep bypass).
source:
  - url: https://github.com/ROCm/rocm-libraries/archive/refs/tags/rocm-${version}.tar.gz
    filename: rocm-rocm-libraries-${version}.tar.gz
    sha256: 50a4090b4ad18b34c375f666e5f71816cf55f89bc2175dc3eba9a7b80f65a15d

gpu_targets: "gfx1100;gfx1102;gfx1201"

dependencies:
  build:
  - cmake
  - ninja
  - rocm-cmake
  - rocm-hip
  host: []
  runtime:
  - rocm-hip
verify_paths:
  - /opt/rocm/lib/librocrand.so
  - /opt/rocm/include/rocrand/rocrand.h
