name: xmodmap
version: 1.0.11
release: 1
description: Keyboard modifier utility
license: MIT
homepage: ''
tier: desktop
build_style: autotools
install_func: do_install
source:
- url: https://www.x.org/pub/individual/app/xmodmap-${version}.tar.xz
  sha256: NEEDS_CHECKSUM
dependencies:
  build:
  - libX11
  host: []
  runtime: []
configure_flags:
- --prefix=/usr
