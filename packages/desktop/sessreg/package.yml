name: sessreg
version: 1.1.3
release: 1
description: Manage utmpx/wtmpx entries for non-init sessions
license: MIT
homepage: ''
tier: desktop
build_style: autotools
install_func: do_install
source:
- url: https://www.x.org/pub/individual/app/sessreg-${version}.tar.xz
  sha256: NEEDS_CHECKSUM
dependencies:
  build:
  - xorgproto
  host: []
  runtime: []
configure_flags:
- --prefix=/usr
