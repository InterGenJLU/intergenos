name: libXtst
version: 1.2.5
release: 1
description: X Test extension library
license: MIT
homepage: https://www.x.org/
tier: desktop
build_style: autotools
install_func: do_install
source:
- url: https://www.x.org/pub/individual/lib/libXtst-${version}.tar.xz
  sha256: NEEDS_CHECKSUM
dependencies:
  build:
  - libXi
  host: []
  runtime: []
configure_flags:
- --prefix=/usr
- --disable-static
