#!/bin/bash
# alsa-plugins 1.2.12 — ALSA plugin collection
# BLFS 13.0

configure() {
    ./configure --prefix=/usr
}

build() {
    make -j${IGOS_JOBS}
}

do_install() {
    make DESTDIR="$DESTDIR" install
}
