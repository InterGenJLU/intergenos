name: xset
version: 1.2.5
release: 1
description: User preference utility for X
license: MIT
homepage: ''
tier: desktop
build_style: autotools
install_func: do_install
source:
- url: https://www.x.org/pub/individual/app/xset-${version}.tar.xz
  sha256: NEEDS_CHECKSUM
dependencies:
  build:
  - libXmu
  - libXext
  host: []
  runtime: []
configure_flags:
- --prefix=/usr
