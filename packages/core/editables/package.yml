name: editables
version: '0.5'
release: 2  # r2: pip install adds --ignore-installed — populated-chroot rebuild empty-archive class sweep (DFB-01/02, 2026-07-19).
description: Python editable installs helper
license: MIT
homepage: ''
tier: core
build_style: custom
install_func: do_install
source:
- url: https://files.pythonhosted.org/packages/source/e/editables/editables-${version}.tar.gz
  sha256: 309627d9b5c4adc0e668d8c6fa7bac1ba7c8c5d415c2d27f60f081f8e80d1de2
dependencies:
  build:
  # The pinned sdist's [build-system] names flit_core.buildapi, provided by
  # flit-core. build.sh builds with --no-build-isolation, which installs nothing
  # for the build, so flit-core must already be present; declaring it here is
  # what orders it ahead of this package.
  - flit-core
  host: []
  runtime: []
verify_paths:
  - /usr/lib/python3.14/site-packages/editables
